# Go build
*.exe
*.test
*.out
bin/
dist/
coverage.out
coverage-full.out
/kacho-registry
/kacho-migrator

# Logs / OS
*.log
.DS_Store

# IDE
.idea/
.vscode/

# Local polyrepo override (not committed — используется для standalone-сборки)
go.work
go.work.sum

# Синканная AI-оснастка (управляется ./sync-tooling.sh)
.claude/

# AI-оснастка (источник истины — kacho-workspace, локально синкается) — не коммитим
.claude/
CLAUDE.md
AGENTS.md
.codex/
.agents/
